name: Deploy Mondal's E-Commerce

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to Ubuntu Server via SSH
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout the code
      - name: Git Checkout
        uses: actions/checkout@v4

      # 2️⃣ Deploy via SSH directly to your server
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}       # ssh.needyamin.site
          port: 22                             # your server SSH port
          username: ${{ secrets.SSH_USER }}   # needyamin
          key: ${{ secrets.SSH_PRIVATE_KEY }} # your private SSH key
          script: |
            # Navigate to your project
            cd ~/mondals-ecommerce || cd /var/www/mondals-ecommerce || (echo "❌ Project directory not found!" && exit 1)

            # Make deploy script executable and run it
            chmod +x scripts/deploy.sh
            ./scripts/deploy.sh